services:
  leaf_api:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: leaf_api
    ports:
      - "5000:5000"
    volumes:
      - .:/app
    environment:
      - PYTHONUNBUFFERED=1
    restart: always